# Watchlist

Copy to `$MANGO_HOME/watchlist.md` (default `~/.mango/watchlist.md`).

A single pipe table. `Notes` is free text — it is shown back to you and never
parsed, so use it for whatever you need to remember about the name.

| Symbol | Name       | Notes                          |
|--------|------------|--------------------------------|
| NVDA   | NVIDIA     | AI infrastructure              |
| MSFT   | Microsoft  | Enterprise + AI                |
